import { ABILITY_KEYS, ABILITY_LABELS } from "../utils/characterCreation";
import { PROFESSION_DEFINITIONS } from "../utils/craftingProfessions";
import { automaticCastingAbilityLabel, bestEligibleCastingAbility } from "../utils/playerForgeAutomaticCasting";
import { countStartingSpellSelections } from "../utils/playerForgeRules";
import { classChoiceSelectionSummary, useNpcForgeClassChoice } from "./NpcForgeClassChoiceContext";
import { sourceChoiceSelectionSummary, useNpcForgeSourceChoices } from "./NpcForgeSourceChoiceContext";
import { speciesFeatChoicesFromState, speciesSkillChoicesFromState, useNpcForgeSpeciesChoices } from "./NpcForgeSpeciesChoiceContext";

function modifier(score) { const value = Math.floor((Number(score || 10) - 10) / 2); return value >= 0 ? `+${value}` : String(value); }
function ReviewSection({ title, stepKey, onEdit, children }) { return <section className="npc-forge-review-section"><header><span>{title}</span><button type="button" onClick={() => onEdit?.(stepKey)}>Edit</button></header>{children}</section>; }
function ChoiceList({ choices = [], empty = "No permanent class choices at this level." }) { return choices.length ? <div className="npc-forge-review-choice-list">{choices.map((entry) => <div key={`${entry.groupId}-${entry.key}`}><strong>{entry.groupLabel}</strong><span>{entry.name}</span><small>{entry.source || "Campaign"}</small></div>)}</div> : <p className="npc-forge-review-note">{empty}</p>; }
function SourceChoiceList({ choices = [], empty = "No additional source-owned choices." }) { return choices.length ? <div className="npc-forge-review-choice-list">{choices.map((entry) => <div key={`${entry.groupId}-${entry.fieldId}-${entry.key}`}><strong>{entry.groupLabel}</strong><span>{entry.label}</span><small>{entry.source || "Campaign"}</small></div>)}</div> : <p className="npc-forge-review-note">{empty}</p>; }
function allowedCastingAbilities(group = {}) {
  if (Array.isArray(group.metadata?.allowedCastingAbilities)) return group.metadata.allowedCastingAbilities;
  return [...new Set((group.fields || []).flatMap((field) => Array.isArray(field.metadata?.allowedCastingAbilities) ? field.metadata.allowedCastingAbilities : []))];
}

export default function NpcForgeReviewPanel({ playerMode, draft, createPayload, selectedSpecies, selectedBackground, selectedClass, selectedSubclass, selectedBackgroundFeat, speciesBonusFeat, selectedSkillKeys, selectedClassSkills, backgroundSkills, selectedTrainedProfessions, spellRows, spellSelections, spellModel, finalAbilities, proficiencyBonus, dynamicHp, classHitDie, titleForSkill, assetSummary, onEdit }) {
  const { state: speciesChoiceState } = useNpcForgeSpeciesChoices();
  const { state: classChoiceState } = useNpcForgeClassChoice();
  const { state: sourceChoiceState } = useNpcForgeSourceChoices();
  const speciesSkillChoices = speciesSkillChoicesFromState(speciesChoiceState);
  const speciesFeatChoices = speciesFeatChoicesFromState(speciesChoiceState);
  const classFeatureChoices = classChoiceSelectionSummary(classChoiceState);
  const classProgressionChoices = classFeatureChoices.filter((entry) => (entry.placement || "class") === "class");
  const trainingClassChoices = classFeatureChoices.filter((entry) => entry.placement === "training");
  const sourceChoices = sourceChoiceSelectionSummary(sourceChoiceState);
  const originLanguages = [...new Set(["Common", ...sourceChoices.filter((entry) => entry.kind === "language").map((entry) => entry.label)])];
  const sourceSize = sourceChoices.find((entry) => entry.kind === "size")?.label || draft.size || "Source default";
  const backgroundSourceChoices = sourceChoices.filter((entry) => entry.placement === "background");
  const trainingSourceChoices = sourceChoices.filter((entry) => entry.placement === "training");
  const trainingDecisionSourceChoices = sourceChoices.filter((entry) => ["class", "advancement"].includes(entry.placement) && !["feat", "boon", "boon-or-feat"].includes(entry.kind));
  const sourceMagicChoices = sourceChoices.filter((entry) => entry.placement === "spells" && entry.kind === "spell");
  const automaticSourceMagic = (sourceChoiceState.groups || []).filter((group) => group.placement === "spells").flatMap((group) => {
    const allowed = allowedCastingAbilities(group);
    if (!allowed.length) return [];
    const result = bestEligibleCastingAbility(finalAbilities, allowed, selectedClass?.spellcasting_ability || "");
    return result ? [{ groupId: group.id, label: group.label, result, allowed }] : [];
  });
  const spellCounts = countStartingSpellSelections(spellRows, spellSelections);
  const selectedSpells = spellRows.filter((spell) => spellSelections?.[spell.id]);
  const feats = [...new Set([selectedBackgroundFeat?.name, speciesBonusFeat?.name, ...speciesFeatChoices.map((entry) => entry.label), ...classFeatureChoices.filter((entry) => entry.groupKind === "fighting-style" || entry.kind === "feat").map((entry) => entry.name), ...(draft.additionalFeats || [])].filter(Boolean))];
  const allSkillKeys = [...new Set([...selectedSkillKeys, ...speciesSkillChoices.map((entry) => entry.value)])];
  return <div className="npc-forge-section npc-forge-review-dossier"><div className="npc-forge-section-heading"><div><span>Review</span><h3>{playerMode ? "Confirm your player character" : "Confirm the canonical character"}</h3></div><p>Review every rules source before creation. Use Edit to return directly to the owning step.</p></div>
    <section className="npc-forge-review-banner">{draft.portraitUrl ? <img src={draft.portraitUrl} alt="Selected character portrait" /> : <div className="npc-forge-review-banner__empty">Portrait required</div>}<div><span>{playerMode ? "Player Character" : draft.kind === "merchant" ? "Merchant" : "NPC"}</span><h2>{createPayload.name || "Unnamed Character"}</h2><p>{selectedSpecies?.name || "Species"} • {selectedBackground?.name || "Background"} • {selectedClass?.class_name || "Class"} level {draft.level}{selectedSubclass?.name ? ` • ${selectedSubclass.name}` : ""}</p><div><b>PB +{proficiencyBonus}</b><b>{dynamicHp} HP</b><b>{draft.level}d{classHitDie}</b>{selectedClass?.spellcasting_ability || sourceMagicChoices.length ? <b>Magic</b> : null}</div></div></section>
    <div className="npc-forge-review-dossier__grid">
      <ReviewSection title="Origin & Identity" stepKey="species" onEdit={onEdit}><div className="npc-forge-review-copy"><strong>{draft.name}</strong><p>{draft.role || selectedClass?.class_name}{draft.affiliation ? ` • ${draft.affiliation}` : ""}</p><p>{draft.description || "No immediate description entered."}</p></div><dl><div><dt>Species</dt><dd>{selectedSpecies?.name || "—"}</dd></div><div><dt>Size</dt><dd>{sourceSize}</dd></div><div><dt>Background</dt><dd>{selectedBackground?.name || "—"}</dd></div><div><dt>Alignment</dt><dd>{draft.alignment || "—"}</dd></div><div><dt>Languages</dt><dd>{playerMode ? originLanguages.join(", ") : draft.languagesText || "—"}</dd></div>{speciesSkillChoices.length ? <div><dt>Species skill</dt><dd>{speciesSkillChoices.map((entry) => entry.label).join(", ")}</dd></div> : null}{speciesFeatChoices.length ? <div><dt>Species feat</dt><dd>{speciesFeatChoices.map((entry) => entry.label).join(", ")}</dd></div> : null}{backgroundSourceChoices.length ? <div><dt>Background choices</dt><dd>{backgroundSourceChoices.map((entry) => `${entry.fieldLabel}: ${entry.label}`).join("; ")}</dd></div> : null}</dl></ReviewSection>
      <ReviewSection title="Class Progression" stepKey="class" onEdit={onEdit}><div className="npc-forge-review-copy"><strong>{selectedClass?.class_name || "—"} {draft.level}</strong><p>{selectedSubclass?.name ? `${selectedSubclass.name} • ${selectedSubclass.source}` : "No subclass selected at this level."}</p><p>{selectedClass?.summary}</p></div><dl><div><dt>Hit Die</dt><dd>d{classHitDie}</dd></div><div><dt>Primary</dt><dd>{(selectedClass?.primary_abilities || []).map((key) => ABILITY_LABELS[key]).join(", ") || "Varies"}</dd></div><div><dt>Saves</dt><dd>{(selectedClass?.saving_throws || []).map((key) => ABILITY_LABELS[key]).join(", ") || "Varies"}</dd></div></dl><ChoiceList choices={classProgressionChoices} /></ReviewSection>
      <ReviewSection title="Ability Scores" stepKey="abilities" onEdit={onEdit}><div className="npc-forge-review-abilities">{ABILITY_KEYS.map((key) => <div key={key}><span>{ABILITY_LABELS[key]}</span><strong>{finalAbilities[key]}</strong><small>{modifier(finalAbilities[key])}</small></div>)}</div><p className="npc-forge-review-note">Generation method: {draft.abilityMethod}. Species Bonus: {draft.speciesBonus?.mode === "feat" ? speciesBonusFeat?.name || "feat not chosen" : draft.speciesBonus?.mode === "three" ? "+1 to three abilities" : "+2 and +1"}.</p></ReviewSection>
      <ReviewSection title="Training & Professions" stepKey="training" onEdit={onEdit}><dl><div><dt>Background skills</dt><dd>{backgroundSkills.map(titleForSkill).join(", ") || "None"}</dd></div><div><dt>Class choices</dt><dd>{selectedClassSkills.map(titleForSkill).join(", ") || "None"}</dd></div>{speciesSkillChoices.length ? <div><dt>Species choice</dt><dd>{speciesSkillChoices.map((entry) => entry.label).join(", ")}</dd></div> : null}<div><dt>All trained skills</dt><dd>{allSkillKeys.map(titleForSkill).join(", ") || "None"}</dd></div>{trainingSourceChoices.length ? <div><dt>Source tools</dt><dd>{trainingSourceChoices.map((entry) => entry.label).join(", ")}</dd></div> : null}<div><dt>Professions</dt><dd>{selectedTrainedProfessions.map((key) => PROFESSION_DEFINITIONS[key]?.label || key).join(", ") || "None"}</dd></div></dl><ChoiceList choices={trainingClassChoices} empty="No additional Training-owned class choices." /></ReviewSection>
      <ReviewSection title="Feats & Class Abilities" stepKey="training" onEdit={onEdit}><div className="npc-forge-review-chip-list">{feats.length ? feats.map((feat) => <span key={feat}>{feat}</span>) : <em>No starting feats listed.</em>}</div><SourceChoiceList choices={trainingDecisionSourceChoices} empty="No additional source-owned class ability selections." /><p className="npc-forge-review-note">Background, Species, feat, and class-granted choices retain distinct sources on the generated sheet.</p></ReviewSection>
      <ReviewSection title="Starting Magic" stepKey="spells" onEdit={onEdit}><div className="npc-forge-review-copy"><strong>{selectedClass?.spellcasting_ability ? `${selectedClass.class_name} spellcasting` : sourceMagicChoices.length ? "Source-owned magic" : "No base-class spell selection"}</strong>{spellModel?.mode !== "none" ? <p>{spellCounts.cantrips} cantrips • {spellCounts.leveled} leveled spells{spellModel?.mode === "spellbook" ? ` • ${spellCounts.prepared - spellCounts.cantrips} prepared` : ""}</p> : <p>Species, feat, background, subclass, or class features can still grant magic.</p>}</div>{selectedSpells.length ? <div className="npc-forge-review-spells">{selectedSpells.map((spell) => <span key={spell.id}>{spell.name}{spellSelections[spell.id]?.prepared && Number(spell.level) > 0 ? " • prepared" : ""}</span>)}</div> : null}{sourceMagicChoices.length ? <SourceChoiceList choices={sourceMagicChoices} empty="No source-owned spell selections." /> : null}{automaticSourceMagic.length ? <div className="npc-forge-review-auto-magic">{automaticSourceMagic.map((entry) => <div key={entry.groupId}><strong>{entry.label}</strong><span>{automaticCastingAbilityLabel(entry.result)}</span><small>Automatically selected from {entry.allowed.map((key) => key.toUpperCase()).join(" / ")} using final ability scores.</small></div>)}</div> : null}</ReviewSection>
      <ReviewSection title="Story & Campaign Hooks" stepKey="story" onEdit={onEdit}><dl><div><dt>Motivation</dt><dd>{draft.motivation || "Not entered"}</dd></div><div><dt>Personality</dt><dd>{draft.personalityTraits || "Not entered"}</dd></div><div><dt>Ideal</dt><dd>{draft.ideals || "Not entered"}</dd></div><div><dt>Bond</dt><dd>{draft.bonds || "Not entered"}</dd></div><div><dt>Flaw</dt><dd>{draft.flaws || "Not entered"}</dd></div></dl></ReviewSection>
      <ReviewSection title="Campaign Status" stepKey="identity" onEdit={onEdit}><dl><div><dt>Ownership</dt><dd>{playerMode ? "Player-owned" : "Campaign NPC"}</dd></div><div><dt>Map placement</dt><dd>{playerMode ? "Off-map at creation" : draft.locationId || "Not listed"}</dd></div><div><dt>Portrait</dt><dd>{draft.portraitName || "Not selected"}</dd></div><div><dt>Visual asset</dt><dd>{assetSummary(draft.spriteAsset)}</dd></div></dl></ReviewSection>
    </div><details className="npc-forge-json mt-3"><summary>Review generated sheet JSON</summary><pre>{JSON.stringify(createPayload.sheet, null, 2)}</pre></details><style jsx global>{`
      .npc-forge-review-banner{display:grid;grid-template-columns:130px minmax(0,1fr);gap:18px;align-items:center;padding:18px;border:1px solid rgba(168,108,255,.38);border-radius:15px;background:linear-gradient(110deg,rgba(62,31,95,.72),rgba(12,29,39,.88))}.npc-forge-review-banner img,.npc-forge-review-banner__empty{width:130px;height:170px;border-radius:10px;object-fit:cover;border:1px solid rgba(255,255,255,.14);background:#090b12}.npc-forge-review-banner__empty{display:grid;place-items:center;color:rgba(255,255,255,.5);text-align:center}.npc-forge-review-banner>div:last-child{display:grid;gap:7px}.npc-forge-review-banner span,.npc-forge-review-section>header>span{color:#d7bfff;font-size:.64rem;font-weight:900;letter-spacing:.09em;text-transform:uppercase}.npc-forge-review-banner h2{margin:0;color:#fff;font-size:clamp(1.7rem,3vw,2.5rem)}.npc-forge-review-banner p{margin:0;color:rgba(255,255,255,.75)}.npc-forge-review-banner>div>div{display:flex;gap:7px;flex-wrap:wrap}.npc-forge-review-banner b{padding:4px 8px;border-radius:999px;background:rgba(88,214,199,.16);color:#c6fff7;font-size:.68rem}.npc-forge-review-dossier__grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-top:14px}.npc-forge-review-section{display:grid;align-content:start;gap:11px;padding:14px;border:1px solid rgba(255,255,255,.1);border-radius:12px;background:linear-gradient(145deg,rgba(28,22,39,.88),rgba(11,14,22,.94))}.npc-forge-review-section>header{display:flex;justify-content:space-between;gap:10px;padding-bottom:8px;border-bottom:1px solid rgba(255,255,255,.08)}.npc-forge-review-section>header button{padding:3px 8px;border:1px solid rgba(168,108,255,.38);border-radius:6px;color:#eadfff;background:rgba(126,72,199,.1);font-size:.63rem}.npc-forge-review-section dl{display:grid;gap:7px;margin:0}.npc-forge-review-section dl>div{display:grid;grid-template-columns:110px minmax(0,1fr);gap:8px}.npc-forge-review-section dt{color:rgba(255,255,255,.48);font-size:.65rem;text-transform:uppercase}.npc-forge-review-section dd{margin:0;color:#fff;font-size:.74rem}.npc-forge-review-copy{display:grid;gap:5px}.npc-forge-review-copy strong{color:#fff}.npc-forge-review-copy p,.npc-forge-review-note{margin:0;color:rgba(255,255,255,.68);font-size:.73rem;line-height:1.5}.npc-forge-review-abilities{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}.npc-forge-review-abilities>div{display:grid;grid-template-columns:1fr auto;gap:2px 7px;padding:8px;border:1px solid rgba(255,255,255,.08);border-radius:8px}.npc-forge-review-abilities span{color:rgba(255,255,255,.55);font-size:.58rem;text-transform:uppercase}.npc-forge-review-abilities strong{grid-row:1/3;grid-column:2;color:#fff;font-size:1.1rem}.npc-forge-review-abilities small{color:#cfb4f7}.npc-forge-review-chip-list,.npc-forge-review-spells{display:flex;flex-wrap:wrap;gap:6px}.npc-forge-review-chip-list span,.npc-forge-review-spells span{padding:5px 8px;border:1px solid rgba(168,108,255,.3);border-radius:999px;color:#eadfff;background:rgba(126,72,199,.1);font-size:.66rem}.npc-forge-review-choice-list,.npc-forge-review-auto-magic{display:grid;gap:6px}.npc-forge-review-choice-list>div{display:grid;grid-template-columns:minmax(110px,.8fr) minmax(0,1.3fr) auto;gap:8px;padding:7px 8px;border:1px solid rgba(88,214,199,.18);border-radius:8px;background:rgba(88,214,199,.045)}.npc-forge-review-choice-list strong{color:#9ff8ec;font-size:.64rem}.npc-forge-review-choice-list span{color:#fff;font-size:.7rem}.npc-forge-review-choice-list small{color:rgba(255,255,255,.45);font-size:.58rem}.npc-forge-review-auto-magic>div{display:grid;gap:2px;padding:8px 9px;border:1px solid rgba(88,214,199,.22);border-radius:8px;background:rgba(88,214,199,.05)}.npc-forge-review-auto-magic strong{color:#9ff8ec;font-size:.66rem}.npc-forge-review-auto-magic span{color:#fff;font-size:.72rem}.npc-forge-review-auto-magic small{color:rgba(255,255,255,.48);font-size:.58rem}@media(max-width:900px){.npc-forge-review-dossier__grid{grid-template-columns:1fr}}@media(max-width:620px){.npc-forge-review-banner{grid-template-columns:1fr}.npc-forge-review-banner img,.npc-forge-review-banner__empty{width:96px;height:128px}.npc-forge-review-abilities{grid-template-columns:repeat(2,minmax(0,1fr))}.npc-forge-review-choice-list>div{grid-template-columns:1fr}}
    `}</style></div>;
}
