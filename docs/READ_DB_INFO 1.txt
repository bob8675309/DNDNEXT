
    world_state
    characters
    map_routes
    map_route_points
    map_route_edges

and any RLS policies (none of those five tables have RLS enabled in your project — I verified schema metadata). I included related FK constraints and indexes as present in your database, plus sequence defaults and CHECK constraints. Execute this in the Supabase SQL editor to recreate the schema/tables (or to keep as authoritative documentation).

Note: I validated that in your public schema the five tables exist and have RLS disabled. If you want RLS enabled, tell me which auth rules you want and I’ll add policies.

-- Begin SQL (paste into Supabase SQL editor) --

-- world_state DROP TABLE IF EXISTS public.world_state CASCADE; CREATE TABLE public.world_state ( id integer PRIMARY KEY DEFAULT 1, world_time timestamptz NOT NULL DEFAULT now(), time_scale double precision NOT NULL DEFAULT 1, updated_at timestamptz, seed bigint DEFAULT 1337 );

-- characters DROP TABLE IF EXISTS public.characters CASCADE; CREATE TABLE public.characters ( id uuid PRIMARY KEY DEFAULT gen_random_uuid(), name text UNIQUE, race text, role text, description text, motivation text, quirk text, mannerism text, voice text, secret text, affiliation text, status text NOT NULL DEFAULT 'alive' CHECK (status = ANY (ARRAY['alive','dead','missing','unknown'])), background text, tags text[] DEFAULT '{}'::text[], kind text NOT NULL DEFAULT 'npc' CHECK (kind = ANY (ARRAY['npc','merchant'])), storefront_enabled boolean NOT NULL DEFAULT false, map_icon_id uuid, x double precision NOT NULL DEFAULT 0, y double precision NOT NULL DEFAULT 0, location_id bigint, last_known_location_id bigint, projected_destination_id bigint, roaming_speed double precision NOT NULL DEFAULT 0, is_hidden boolean NOT NULL DEFAULT false, state text NOT NULL DEFAULT 'resting' CHECK (state = ANY (ARRAY['moving','resting','excursion','hidden','camping'])), rest_until timestamptz, route_id bigint, route_point_seq integer NOT NULL DEFAULT 1, prev_point_seq integer, route_segment_progress double precision NOT NULL DEFAULT 0, last_moved_at timestamptz NOT NULL DEFAULT timezone('utc', now()), route_mode text NOT NULL DEFAULT 'trade', current_point_seq integer, next_point_seq integer, segment_started_at timestamptz, segment_ends_at timestamptz, storefront_title text, storefront_tagline text, storefront_bg_url text, storefront_bg_video_url text, storefront_bg_image_url text, updated_at timestamptz NOT NULL DEFAULT timezone('utc', now()), home_location_id bigint, sprite_key text, map_scale numeric, sprite_path text, sprite_scale double precision, move_speed_units_per_hour numeric NOT NULL DEFAULT 1.0, dwell_hours integer DEFAULT 6 CHECK (dwell_hours IS NULL OR dwell_hours >= 1 AND dwell_hours <= 24), dwell_started_at timestamptz, dwell_ends_at timestamptz, next_action_at timestamptz, tick_jitter_seconds integer NOT NULL DEFAULT 0, paused_state text, paused_remaining_seconds integer, camp_reason text, camp_started_at timestamptz, camp_sprite_path text );

-- map_routes DROP TABLE IF EXISTS public.map_routes CASCADE; CREATE TABLE public.map_routes ( id bigint PRIMARY KEY DEFAULT nextval('map_routes_id_seq'::regclass), name text, code text UNIQUE, route_type text NOT NULL DEFAULT 'trade' CHECK (route_type = ANY (ARRAY['trade','excursion','adventure'])), color text, is_loop boolean NOT NULL DEFAULT true, created_at timestamptz NOT NULL DEFAULT timezone('utc', now()), is_visible boolean NOT NULL DEFAULT false );

-- map_route_points DROP TABLE IF EXISTS public.map_route_points CASCADE; CREATE TABLE public.map_route_points ( id bigint PRIMARY KEY DEFAULT nextval('map_route_points_id_seq'::regclass), route_id bigint NOT NULL, seq integer, x double precision, y double precision, location_id bigint, dwell_seconds double precision NOT NULL DEFAULT 0 );

-- map_route_edges DROP TABLE IF EXISTS public.map_route_edges CASCADE; CREATE TABLE public.map_route_edges ( id bigint PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, route_id bigint, a_point_id bigint, b_point_id bigint, created_at timestamptz NOT NULL DEFAULT now() );

-- Optional: map_route_segments (present in DB) DROP TABLE IF EXISTS public.map_route_segments CASCADE; CREATE TABLE public.map_route_segments ( id bigint PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, route_id bigint, a_point_id bigint, b_point_id bigint, created_at timestamptz NOT NULL DEFAULT timezone('utc', now()) );

-- Recreate foreign key constraints found in your DB -- characters -> locations, map_icons, map_routes, locations (home/last/projected) ALTER TABLE public.characters ADD CONSTRAINT characters_map_icon_id_fkey FOREIGN KEY (map_icon_id) REFERENCES public.map_icons (id), ADD CONSTRAINT characters_home_location_id_fkey FOREIGN KEY (home_location_id) REFERENCES public.locations (id), ADD CONSTRAINT characters_location_id_fkey FOREIGN KEY (location_id) REFERENCES public.locations (id), ADD CONSTRAINT characters_last_known_location_id_fkey FOREIGN KEY (last_known_location_id) REFERENCES public.locations (id), ADD CONSTRAINT characters_projected_destination_id_fkey FOREIGN KEY (projected_destination_id) REFERENCES public.locations (id), ADD CONSTRAINT characters_route_id_fkey FOREIGN KEY (route_id) REFERENCES public.map_routes (id);

-- map_route_points -> map_routes, locations ALTER TABLE public.map_route_points ADD CONSTRAINT map_route_points_route_id_fkey FOREIGN KEY (route_id) REFERENCES public.map_routes (id), ADD CONSTRAINT map_route_points_location_id_fkey FOREIGN KEY (location_id) REFERENCES public.locations (id);

-- map_route_edges -> map_routes, map_route_points (a_point_id, b_point_id) ALTER TABLE public.map_route_edges ADD CONSTRAINT map_route_edges_route_id_fkey FOREIGN KEY (route_id) REFERENCES public.map_routes (id), ADD CONSTRAINT map_route_edges_a_point_id_fkey FOREIGN KEY (a_point_id) REFERENCES public.map_route_points (id), ADD CONSTRAINT map_route_edges_b_point_id_fkey FOREIGN KEY (b_point_id) REFERENCES public.map_route_points (id);

-- map_route_segments -> map_routes, map_route_points (present in DB) ALTER TABLE public.map_route_segments ADD CONSTRAINT map_route_segments_route_id_fkey FOREIGN KEY (route_id) REFERENCES public.map_routes (id), ADD CONSTRAINT map_route_segments_a_point_id_fkey FOREIGN KEY (a_point_id) REFERENCES public.map_route_points (id), ADD CONSTRAINT map_route_segments_b_point_id_fkey FOREIGN KEY (b_point_id) REFERENCES public.map_route_points (id);

-- Indexes (create ones implied by your DB if needed) CREATE INDEX IF NOT EXISTS idx_map_route_points_route_id ON public.map_route_points (route_id); CREATE INDEX IF NOT EXISTS idx_map_route_edges_route_id ON public.map_route_edges (route_id);

-- CHECK constraints (already included inline) — re-create for clarity ALTER TABLE public.map_routes ADD CONSTRAINT map_routes_route_type_check CHECK (route_type = ANY (ARRAY['trade','excursion','adventure'])); ALTER TABLE public.characters ADD CONSTRAINT characters_status_check CHECK (status = ANY (ARRAY['alive','dead','missing','unknown'])); ALTER TABLE public.characters ADD CONSTRAINT characters_kind_check CHECK (kind = ANY (ARRAY['npc','merchant'])); ALTER TABLE public.characters ADD CONSTRAINT characters_state_check CHECK (state = ANY (ARRAY['moving','resting','excursion','hidden','camping'])); ALTER TABLE public.map_routes ADD CONSTRAINT map_routes_is_loop_default_check CHECK (is_loop IN (true, false));

-- Sequences (if not present, create) CREATE SEQUENCE IF NOT EXISTS map_routes_id_seq; CREATE SEQUENCE IF NOT EXISTS map_route_points_id_seq;

-- Grants: keep default ownership; Supabase uses RLS for auth. No additional grants added here.

-- RLS policies -- Verified: RLS is disabled on these tables in your DB metadata. -- If you want RLS enabled for any of these tables, here is an example template you can adapt:

/* -- Example: enable RLS and allow authenticated users read-only access to visible map routes ALTER TABLE public.map_routes ENABLE ROW LEVEL SECURITY;*

CREATE POLICY "authenticated_can_read_visible_routes" ON public.map_routes FOR SELECT TO authenticated USING (is_visible);

-- Example: characters owned by a player (if characters had an owner column user_id:uuid) ALTER TABLE public.characters ENABLE ROW LEVEL SECURITY; CREATE POLICY "players_own_characters" ON public.characters FOR ALL TO authenticated USING ((SELECT auth.uid()) = owner_user_id) WITH CHECK ((SELECT auth.uid()) = owner_user_id); /

-- End SQL

If you'd like:

    I can produce the exact CREATE SEQUENCE options (start/owned by) copied from the DB — tell me if you want that.
    I can enable and add RLS policies tailored to your auth model (e.g., characters owned by players, or only staff can modify routes). Tell me the desired access rules and I’ll add precise policy SQL.
