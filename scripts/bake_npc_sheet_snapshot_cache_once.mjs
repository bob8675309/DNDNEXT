import fs from 'node:fs';
import { execFileSync } from 'node:child_process';
const path='pages/npcs.js';
let s=fs.readFileSync(path,'utf8');
function rep(a,b,label){if(!s.includes(a))throw new Error('Missing anchor: '+label);s=s.replace(a,b)}
rep('  const notesRequestRef = useRef(0);\n  const sheetAbortRef = useRef(null);','  const notesRequestRef = useRef(0);\n  const sheetCacheRef = useRef(new Map());\n  const [sheetCacheReady, setSheetCacheReady] = useState(false);','sheet refs');
rep('  const retrySelectedSheet = useCallback(() => {\n    const normalized = normalizeNpcSelectionKey(selectedKeyRef.current);\n    if (!normalized) return;\n\n    sheetAbortRef.current?.abort();\n    sheetRequestRef.current += 1;\n    setSheetLoadError("");\n    setSheetLoading(true);\n    setSheetReloadToken((value) => value + 1);\n  }, []);','  const retrySelectedSheet = useCallback(() => {\n    const normalized = normalizeNpcSelectionKey(selectedKeyRef.current);\n    if (!normalized) return;\n    const id = parseKey(normalized)?.id;\n    if (id) sheetCacheRef.current.delete(String(id));\n    sheetRequestRef.current += 1;\n    setSheetLoadError("");\n    setSheetLoading(true);\n    setSheetReloadToken((value) => value + 1);\n  }, []);','retry');
rep('    sheetAbortRef.current?.abort();\n    selectedKeyRef.current = normalized;','    selectedKeyRef.current = normalized;','selection abort');
rep('    setSheet(null);\n    setSheetDraft({});\n    setSheetEditMode(false);\n    setEquippedRows([]);\n    setNotes([]);\n    setLastRoll(null);\n    setSheetLoadError("");\n    setSheetLoading(Boolean(normalized));\n    setSelectedKey(normalized || null);','    const selectedId = parseKey(normalized)?.id;\n    const hasCachedSheet = Boolean(selectedId) && sheetCacheRef.current.has(String(selectedId));\n    const cachedSheet = hasCachedSheet ? deepClone(sheetCacheRef.current.get(String(selectedId)) || {}) : null;\n\n    setSheet(cachedSheet);\n    setSheetDraft(cachedSheet ? deepClone(cachedSheet) : {});\n    setSheetEditMode(false);\n    setEquippedRows([]);\n    setNotes([]);\n    setLastRoll(null);\n    setSheetLoadError("");\n    setSheetLoading(Boolean(normalized) && !hasCachedSheet);\n    setSelectedKey(normalized || null);','selection cache');
const start=s.indexOf('  const loadSelectedSheet = useCallback(async (key) => {');
const end=s.indexOf('\n\n  // Load equipped items for NPC/merchant',start);
if(start<0||end<0)throw new Error('Missing sheet loader block');
const loader=`  const loadSelectedSheet = useCallback(async (key, { force = false } = {}) => {\n    const requestedKey = normalizeNpcSelectionKey(key);\n    if (!requestedKey) {\n      setSheetLoadError(\"\");\n      setSheetLoading(false);\n      return null;\n    }\n\n    const id = parseKey(requestedKey)?.id || null;\n    if (!id) {\n      setSheetLoadError(\"The selected character does not have a valid sheet identity.\");\n      setSheetLoading(false);\n      return null;\n    }\n\n    const cacheKey = String(id);\n    const requestId = ++sheetRequestRef.current;\n    const isCurrentRequest = () => isCurrentNpcSelectionRequest({\n      activeKey: selectedKeyRef.current, requestedKey,\n      activeRequestId: sheetRequestRef.current, requestId,\n    });\n\n    if (!force && sheetCacheRef.current.has(cacheKey)) {\n      const cached = deepClone(sheetCacheRef.current.get(cacheKey) || {});\n      if (isCurrentRequest()) {\n        setSheetLoadError(\"\");\n        setSheet(cached);\n        setSheetDraft(deepClone(cached));\n        setSheetEditMode(false);\n        setSheetLoading(false);\n      }\n      return cached;\n    }\n\n    try {\n      const { data, error } = await supabase\n        .from(\"character_sheets\")\n        .select(\"sheet\")\n        .eq(\"character_id\", id)\n        .maybeSingle();\n\n      if (!isCurrentRequest()) return null;\n      if (error) {\n        if (!isSupabaseMissingTable(error)) console.error(error);\n        setSheetLoadError(error.message || \"The selected character sheet could not be loaded.\");\n        return null;\n      }\n      if (!data || !data.sheet || typeof data.sheet !== \"object\") {\n        setSheetLoadError(\"No character sheet record was returned for the selected character.\");\n        return null;\n      }\n\n      const next = deepClone(data.sheet);\n      sheetCacheRef.current.set(cacheKey, deepClone(next));\n      setSheetLoadError(\"\");\n      setSheet(next);\n      setSheetDraft(deepClone(next));\n      setSheetEditMode(false);\n      return next;\n    } catch (error) {\n      if (!isCurrentRequest()) return null;\n      console.error(error);\n      setSheetLoadError(error?.message || \"The selected character sheet could not be loaded.\");\n      return null;\n    } finally {\n      if (isCurrentRequest()) setSheetLoading(false);\n    }\n  }, []);`;
s=s.slice(0,start)+loader+s.slice(end);
const sync='  // Keep the editable draft in sync only when a validated sheet response is\n';
const preload=`  useEffect(() => {\n    let cancelled = false;\n    (async () => {\n      const { data, error } = await supabase\n        .from(\"character_sheets\")\n        .select(\"character_id,sheet\");\n      if (cancelled) return;\n      if (error) {\n        console.error(error);\n        setSheetCacheReady(true);\n        return;\n      }\n      const cache = new Map();\n      for (const row of data || []) {\n        if (row?.character_id && row?.sheet && typeof row.sheet === \"object\") {\n          cache.set(String(row.character_id), deepClone(row.sheet));\n        }\n      }\n      sheetCacheRef.current = cache;\n      setSheetCacheReady(true);\n\n      const activeKey = normalizeNpcSelectionKey(selectedKeyRef.current);\n      const activeId = parseKey(activeKey)?.id;\n      if (activeKey && activeId && cache.has(String(activeId))) {\n        const snapshot = deepClone(cache.get(String(activeId)) || {});\n        setSheet(snapshot);\n        setSheetDraft(deepClone(snapshot));\n        setSheetEditMode(false);\n        setSheetLoadError(\"\");\n        setSheetLoading(false);\n      }\n    })();\n    return () => { cancelled = true; };\n  }, []);\n\n`;
rep(sync,preload+sync,'preload insertion');
rep('    if (!requestedKey) {\n      setSheetLoadError("");\n      setSheetLoading(false);\n      return;\n    }\n\n    void loadSelectedSheet(requestedKey);\n  }, [selectedKey, sheetReloadToken, loadSelectedSheet]);','    if (!requestedKey) {\n      setSheetLoadError("");\n      setSheetLoading(false);\n      return;\n    }\n    if (!sheetCacheReady) return;\n\n    void loadSelectedSheet(requestedKey, { force: sheetReloadToken > 0 });\n  }, [selectedKey, sheetReloadToken, sheetCacheReady, loadSelectedSheet]);','selection effect');
rep('  useEffect(() => {\n    setSheetDraft(deepClone(sheet || {}));\n    setSheetEditMode(false);\n  }, [sheet]);','  useEffect(() => {\n    setSheetDraft(deepClone(sheet || {}));\n    setSheetEditMode(false);\n    const activeId = parseKey(selectedKeyRef.current)?.id;\n    if (activeId && sheet && typeof sheet === "object") {\n      sheetCacheRef.current.set(String(activeId), deepClone(sheet));\n    }\n  }, [sheet]);','cache accepted sheet');
fs.writeFileSync(path,s);
fs.rmSync('scripts/bake_npc_sheet_snapshot_cache_once.mjs');
fs.rmSync('.github/workflows/bake-npc-sheet-snapshot-cache-once.yml');
execFileSync('git',['config','user.name','github-actions[bot]']);
execFileSync('git',['config','user.email','41898282+github-actions[bot]@users.noreply.github.com']);
execFileSync('git',['add','pages/npcs.js','scripts/bake_npc_sheet_snapshot_cache_once.mjs','.github/workflows/bake-npc-sheet-snapshot-cache-once.yml']);
execFileSync('git',['commit','-m','Use cached NPC sheet snapshots for switching']);
execFileSync('git',['push']);
