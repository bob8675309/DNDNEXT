// NPC Forge detail source mutation is intentionally disabled while the creator
// is being moved to canonical checked-in source. Leaving this transformer as a
// no-op keeps the existing prebuild chain stable and prevents generated-source
// drift from breaking Vercel.
console.log("NPC Forge detail transformer disabled; source remains canonical to the checked-in files.");
