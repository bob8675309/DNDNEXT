name: Phase 1T UI Validation

on:
  pull_request:
    branches: [main]
    paths:
      - "pages/encounters/combat.js"
      - "scripts/validate_tactical_*.mjs"
      - "package.json"
      - ".github/workflows/phase1t-ui-validation.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co
      NEXT_PUBLIC_SUPABASE_ANON_KEY: validation-placeholder
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install --ignore-scripts
      - name: Tactical spell suite
        run: npm run check:tactical-spell-suite
      - name: Next build
        run: npx next build
