name: Vercel maintenance verification smoke

on:
  push:
    branches:
      - agent/vercel-storage-guard-20260914
    paths:
      - ".github/workflows/vercel-maintenance-smoke.yml"

permissions:
  contents: read

jobs:
  verify:
    name: Verify cleanup left no failed previews
    uses: ./.github/workflows/vercel-deployment-maintenance.yml
    with:
      mode: dry-run
      confirmation: ""
    secrets: inherit
